# 安装路径图：哪些AI帮你搞定，哪些需要自己来

> 🟢 绿色 = AI可执行（终端命令/自动排查）
> 🟠 橙色 = 需要你手动操作
> 安装卡住随时说"帮我安装"，AI自动排查

---

## 完整安装流程

```mermaid
flowchart TD
    START([开始安装]) --> A

    subgraph A["一、安装 Obsidian"]
        A1["🟠 下载安装包，双击安装"]
        A2["🟠 创建知识库"]
        A3["🟠 设中文 + 开启第三方插件"]
        A1 --> A2 --> A3
    end

    A --> B

    subgraph B["二、安装 Claudian 插件"]
        B1["🟢 安装 Claude Code CLI\n（AI执行 winget install）"]
        B3["🟢 验证安装\n（AI执行 claude --version）"]
        B4["🟠 安装 BRAT 插件"]
        B5["🟠 用 BRAT 安装 Claudian"]
        B6["🟠 配置 Claude CLI path + API"]
        B1 --> B3 --> B4 --> B5 --> B6
    end

    B --> KEY[/"✅ AI正式入住！\n后面卡住可随时喊AI排查"/]

    KEY --> C
    KEY --> D
    KEY --> E

    subgraph C["三、Nano Banana 插件"]
        C1["🟠 复制插件文件夹"]
        C2["🟠 重启并启用"]
        C3["🟠 获取 Gemini API Key\n（需Google账号）"]
        C1 --> C2 --> C3
    end

    subgraph D["四、Video Forge 插件"]
        D1["🟠 安装 Node.js"]
        D2["🟢 验证 Node.js\n（AI执行 node -v）"]
        D3["🟠 复制插件文件夹"]
        D4["🟢 安装 Remotion 依赖\n（AI执行 npm install）"]
        D5["🟠 配置 Fish Audio API Key\n（fal.ai 可选）"]
        D1 --> D2 --> D3 --> D4 --> D5
    end

    subgraph E["五、Web Clipper"]
        E1["🟠 Chrome商店安装扩展"]
        E2["🟠 配置截图保存路径"]
        E1 --> E2
    end

    C --> F
    D --> F
    E --> F

    subgraph F["六、导入知识库 + Skills"]
        F1["🟠 解压知识库模板ZIP\n拖入Obsidian库根目录"]
        F2["🟠 解压Skills包ZIP\n把.claude文件夹拖入根目录"]
        F3["🟢 验证文件结构\n（AI自动检查）"]
        F1 --> F2 --> F3
    end

    F --> DONE([🎉 安装完成！开始用AI干活])

    style A1 fill:#ff9800,color:#fff
    style A2 fill:#ff9800,color:#fff
    style A3 fill:#ff9800,color:#fff
    style B1 fill:#4caf50,color:#fff
    style B3 fill:#4caf50,color:#fff
    style B4 fill:#ff9800,color:#fff
    style B5 fill:#ff9800,color:#fff
    style B6 fill:#ff9800,color:#fff
    style C1 fill:#ff9800,color:#fff
    style C2 fill:#ff9800,color:#fff
    style C3 fill:#ff9800,color:#fff
    style D1 fill:#ff9800,color:#fff
    style D2 fill:#4caf50,color:#fff
    style D3 fill:#ff9800,color:#fff
    style D4 fill:#4caf50,color:#fff
    style D5 fill:#ff9800,color:#fff
    style E1 fill:#ff9800,color:#fff
    style E2 fill:#ff9800,color:#fff
    style F1 fill:#ff9800,color:#fff
    style F2 fill:#ff9800,color:#fff
    style F3 fill:#4caf50,color:#fff
    style KEY fill:#2196f3,color:#fff
    style DONE fill:#4caf50,color:#fff
```

---

## 统计

| 类型 | 数量 | 占比 |
|------|------|------|
| 🟠 必须手动 | 17步 | 77% |
| 🟢 AI可执行 | 5步 | 23% |

**安装阶段：AI核心价值 = 遇到报错时自动排查诊断**

**装完之后：写文案、剪视频、发布、数据采集 → AI全自动**

---

## 遇到问题怎么办

安装过程中任何一步卡住了，直接在Claudian聊天框里说：

> "帮我安装"

AI会自动读取安装手册，检测你当前环境，告诉你哪里出了问题、怎么解决。

*版本：v1.1 | 2026年3月7日*
